# Most hosting platforms (Render, Railway, etc.) inject a PORT env var
# and expect the app to listen on it.
server.port=${PORT:8080}

# Comma-separated list of frontend origins allowed to call this API.
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

spring.datasource.url=jdbc:mysql://centerbeam.proxy.rlwy.net:50639/railway
spring.datasource.username=root
spring.datasource.password=TYTJwWrsnyaEvTwBzsJBzwcEDGPMdEUQ

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQLDialect

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

# JWT config
# In a real deployment this secret belongs in an environment variable, not source control.
jwt.secret=${JWT_SECRET:c2VjdXJlLXRhc2stbWFuYWdlbWVudC1zeXN0ZW0tand0LXNpZ25pbmcta2V5LWNoYW5nZS1tZS1pbi1wcm9k}
jwt.expiration-ms=86400000

# Logging: keep our own package chatty enough to debug auth/ownership
# issues, keep the framework noise down.
logging.level.taskmanagementsystem=DEBUG
logging.level.org.springframework.security=INFO
logging.pattern.console=%d{HH:mm:ss} %-5level %logger{36} - %msg%n

# Swagger UI: http://localhost:8080/swagger-ui.html
springdoc.swagger-ui.path=/swagger-ui.html