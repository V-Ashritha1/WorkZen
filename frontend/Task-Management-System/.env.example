# Copy this to .env for local overrides, or set it as a build-time
# environment variable in your hosting platform (Vercel/Netlify).
# Leave unset locally — it defaults to http://localhost:8080.
VITE_API_BASE_URL=https://your-backend-url.onrender.com
